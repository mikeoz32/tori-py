[project]
name = "tori-py-persistent-streams-core"
version = "0.1.0"
description = "Framework-neutral contracts for partitioned persistent logs"
readme = "README.md"
requires-python = ">=3.14,<3.15"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.28,<0.12.0"]
build-backend = "uv_build"
